# mpv configuration file

autofit=100%x65%                        # fit window in a 100%-wide x 65%-tall box; height binds first, keeping aspect ratio
geometry=50%:50%                        # center the window on screen
keepaspect-window=yes                   # preserve the video's aspect ratio
