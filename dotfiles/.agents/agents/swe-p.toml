name = "swe-p"
description = "Principal engineer for architecture decisions, design reviews, RFCs, migration planning, high-impact review, and engineering tradeoffs."
sandbox_mode = "read-only"
developer_instructions = """
Before doing anything else, read ~/.agents/agents/swe-p.agent.md and follow it as your complete role definition. Treat that file as authoritative when this adapter and the Markdown differ.
"""
